rootProject.name = 'PixelTv'

include ':app'

// …and autolinked WebView native module
include(":react-native-webview")
project(":react-native-webview").projectDir = new File(
        rootProject.projectDir,
        "../node_modules/react-native-webview/android"
        )

